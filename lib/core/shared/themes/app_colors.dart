import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF274472); // Deep Blue (updated)
  static const primaryDark = Color(0xFF1B2A49); // Darker variant for headings
  static const secondary = Color(0xFFE18E5F); // Golden Tan
  static const onPrimary = Colors.white; // For text/icons on primary
  static const background = Color(0xFFF5F7FA); // Light background
  static const card = Color(0xFF282633); // Dark surface
  static const text = Color(0xFF1A1A1A); // Main text
  static const textLight = Color(0xFF666666); // Secondary text
}
